<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunjia.back.mapper.CommentMapper">

    <insert id="addComment">
        insert into comments (id, user_id, article_id, content, create_time, update_time, status)
        values (#{id}, #{userId}, #{articleId}, #{content}, #{createTime}, #{updateTime}, #{status})
    </insert>
</mapper>